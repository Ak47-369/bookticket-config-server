spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${GITHUB_REPO_URL}
          default-label: main
          search-paths: '*/**'
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_PASSWORD}
          clone-on-start: true
management:
  metrics:
    export:
      otlp:
        enabled: true
loki:
  url: ${LOKI_URL}
  username: ${LOKI_USERNAME}
  password: ${LOKI_PASSWORD}
server:
  port: 8888